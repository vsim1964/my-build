- var values = [];
ul
  each val in values
    li= val
  else
    li There are no values

//- ИЛИ

ul
  each val, index in ['zero', 'one', 'two']
    li= index + ': ' + val

//- ИЛИ

- var n = 0;
ul
  while n < 4
    li= n++